const burger=document.getElementById('burger');const page=document.getElementById('page');const body=document.body;const element=document.querySelectorAll('.modileMenu--item');burger.addEventListener('click',(event)=>{if(body.classList.contains('show-sidebar')){closeSidebar()}else{showSidebar()}});function showSidebar(){const mask=document.createElement('div');mask.classList.add('page__mask');mask.addEventListener('click',closeSidebar);body.appendChild(mask);body.classList.add('show-sidebar')}
function closeSidebar(){body.classList.remove('show-sidebar');document.querySelector('.page__mask').remove()}
for(let elem of element){elem.addEventListener('click',closeSidebar)}